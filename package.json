{
  "name": "galuchat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd server && npm run stop && npm run start && cd ..",
    "start:prod": "cd server && npm run stop && npm run start:prod && cd ..",
    "postinstall": "yarn install:server",
    "install:server": "cd server && yarn && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "shipit-cli": "^2.0.0",
    "shipit-deploy": "^2.4.0"
  }
}
